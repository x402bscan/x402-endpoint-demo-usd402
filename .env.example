# ============================================================================
# x402 Demo Server Configuration
# ============================================================================
# This is a demo server that shows how to use x402 payment protocol.
# All variables are REQUIRED unless marked as optional.
# ============================================================================

# Server Configuration
PORT=3000

# ============================================================================
# PAYMENT CONFIGURATION (REQUIRED)
# ============================================================================

# Your wallet address to receive payments (REQUIRED)
# Replace with your address (EVM compatible)
PAYMENT_ADDRESS=0xYourAddressHere

# ============================================================================
# TOKEN CONFIGURATION (REQUIRED)
# ============================================================================

# Token contract address on the blockchain (REQUIRED)
# Example: USD402 on BSC: 0x602620956eA9a67D72914d4f122733849Fbcc8Fd
TOKEN_ADDRESS=0x602620956eA9a67D72914d4f122733849Fbcc8Fd

# Token decimals (REQUIRED)
# Most tokens use 18 decimals
TOKEN_DECIMALS=18

# Token symbol (REQUIRED)
# Short identifier for the token (e.g., USD1, USDC, DAI)
TOKEN_SYMBOL=USD402

# Token full name (REQUIRED)
# Full name of the token for EIP-712 signing
TOKEN_NAME=X402 Wrapped USD

# Token version (REQUIRED)
# Version string for EIP-712 signing (usually "1")
TOKEN_VERSION=1

# Payment amount in smallest unit (REQUIRED)
# For 18 decimals: 1 token = 1000000000000000000 (1 * 10^18)
# For 6 decimals: 1 token = 1000000 (1 * 10^6)
# Formula: amount * 10^decimals
PAYMENT_AMOUNT=1000000000000000000

# ============================================================================
# NETWORK CONFIGURATION (REQUIRED)
# ============================================================================

# Network identifier (REQUIRED)
# Options: bsc, ethereum, polygon, arbitrum, optimism, base, etc.
NETWORK=bsc

# Network display name (REQUIRED)
# Human-readable network name
NETWORK_NAME=BSC (Binance Smart Chain)

# ============================================================================
# AUTHORIZATION CONFIGURATION (REQUIRED)
# ============================================================================

# Authorization type (REQUIRED)
# Options: "permit" (ERC-2612 permit) or "transfer" (direct transfer)
# Use "permit" for gasless approvals, "transfer" for traditional transfers
AUTHORIZATION_TYPE=transfer

# Facilitator Contract Address (REQUIRED ONLY when using "permit")
# Smart contract address that handles permit-based payments
# Not needed for "transfer" authorization type
# FACILITATOR_CONTRACT=0xaef29E5A6278811Cb811eCdA0910f4e3295EEfa0

# Payment timeout in seconds (REQUIRED)
# How long the payment authorization is valid
# Recommended: 120-600 seconds (2-10 minutes)
MAX_TIMEOUT_SECONDS=120

# ============================================================================
# FACILITATOR SERVICE CONFIGURATION (REQUIRED)
# ============================================================================

# Facilitator service URL (REQUIRED)
# The facilitator verifies and processes payments on the blockchain
# Use x402bscan facilitator service
FACILITATOR_URL=https://facilitator.x402bscan.io

# Optional: CDP API Keys for Coinbase Onramp integration
# CDP_API_KEY_ID=your_secret_api_key_id_here
# CDP_API_KEY_SECRET=your_secret_api_key_secret_here
